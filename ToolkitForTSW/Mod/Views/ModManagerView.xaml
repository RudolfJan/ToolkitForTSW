<Window x:Class="ToolkitForTSW.Mod.Views.ModManagerView" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:core="clr-namespace:System;assembly=mscorlib" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:userControls="clr-namespace:Styles.Library.UserControls;assembly=Styles.Library" 
        xmlns:mod="clr-namespace:ToolkitForTSW.Mod.Models" 
        xmlns:views="clr-namespace:ToolkitForTSW.Mod.Views" 
        mc:Ignorable="d" 
        Title="Mod manager" 
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource AllWindows}">

 
  <ScrollViewer>
    <StackPanel>
      <Border Style="{StaticResource BorderDefault}" Width="700" MinHeight="350">
        <TabControl x:Name="Items" />
      </Border>
      <Border Style="{StaticResource DefaultBorder}">

            <Button Name="CloseForm" HorizontalAlignment="Right" 
                    Style="{StaticResource ButtonOK}" 
                    ToolTip="Close this window">
              Done
            </Button>
       </Border>
    </StackPanel>
  </ScrollViewer>
</Window>