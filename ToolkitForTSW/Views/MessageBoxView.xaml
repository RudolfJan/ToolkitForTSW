<UserControl x:Class="ToolkitForTSW.Views.MessageBoxView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ToolkitForTSW.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="200" d:DesignWidth="400"
             Background="{StaticResource WindowBackground}"
             mc:Ignorable="d">
  <StackPanel>
    <WrapPanel>
      <Image Width="32" Height="32"
             Margin="{StaticResource MarginDefault}"
             Source="../Images/information.png" />
      <TextBlock Text="{Binding Title}" Height="30" Style="{StaticResource TextBlockHeader}"/>
    </WrapPanel>
    <TextBlock Text="{Binding Message}"
    MinHeight="80" Width="390" Margin="5,5,5,5"
               Style="{StaticResource TextBlockDefault}"
               TextWrapping="Wrap"/>
    <Button Name="CloseForm" Style="{StaticResource ButtonOK}">Close</Button>
  </StackPanel>
</UserControl>
